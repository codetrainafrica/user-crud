export default function (state = {}, action) {
  switch (action.type) {
    case 'ADD_USER':
    //code to add user to the state
    case 'DELETE_USER':
    //code to delete user

    default:
      return state;
  }
}
